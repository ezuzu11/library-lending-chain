# build artifacts
*.o
*.out
/build/
/lending_tracker

# generated at runtime — not source
/data/chain.txt

# private key material — never commit
/keys/*.pem
!/keys/.gitkeep

# editor/OS cruft
.DS_Store
*.swp
